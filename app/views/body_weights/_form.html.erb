<%= form_with(model: body_weight, local: true) do |form| %>
  <% if body_weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(body_weight.errors.count, "error") %> prohibited this body_weight from being saved:</h2>

      <ul>
      <% body_weight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight, id: :body_weight_weight %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :body_weight_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
