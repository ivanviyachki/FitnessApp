<%= form_with(model: exercise_category, local: true) do |form| %>
  <% if exercise_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exercise_category.errors.count, "error") %> prohibited this exercise_category from being saved:</h2>

      <ul>
      <% exercise_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :exercise_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
